---
description: Create a conventional git commit using the git-commit skill
---

Execute the git-commit skill workflow: analyze the diff, stage files appropriately, and create a conventional commit. If no arguments are given ($ARGUMENTS), auto-detect type and scope from the diff. Otherwise prioritize the user-provided parameters.
